# ScientificCalculator
A desktop scientific calculator built with C++ and Qt Widgets.  The application supports standard arithmetic operations, parentheses, modulo, constants, memory operations and selected scientific functions such as square root, factorial, logarithms and trigonometry.   It also includes input validation, error handling and a custom expression parser.
